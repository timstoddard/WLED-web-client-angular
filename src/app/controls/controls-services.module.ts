import { NgModule } from '@angular/core';

/** This module is used exclusively to provide all the services used by `ControlsComponent` and its child components. */
@NgModule({})
export class ControlsServicesModule { }
