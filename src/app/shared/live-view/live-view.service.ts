import { Injectable } from '@angular/core';
import { LiveViewModule } from './live-view.module';

@Injectable({ providedIn: LiveViewModule })
export class LiveViewService {
  // TODO is this needed?
}
